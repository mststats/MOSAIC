*) replace L with A in all code
*) parallelise compressed_grid.cpp?
*) help file for run_mosaic only; re-use README.txt
*) shapeit inputs OR a conversion tool packaged with MOSAIC
*) read in one SNP at a time for all individuals to reduce memory usage and speed up gridding
*) add citation when MOSAIC is called (once accepted).
*) don't grab all memory by default on a cluster: see Busby email.
	*) remove or reduce gfbs memory footprint
*) pass indices of individuals to MOSAIC rather than -n and -i
*) Fst and Mu map plots?

*) replace all tmp= and tmp$ with attach() calls (neater) in run.R only
*) fix all FLAGs
*) reduce imports() and replace with importFrom calls
*) functionize: see tofunc.txt
*) Edit create_package.sh to include DESCRIPTION, etc
*) shapeit input
*) flags for command line options e.g. --MC 16 etc
*) don't grab all memory by default on a cluster: see Busby email.
